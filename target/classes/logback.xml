<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 控制台输出 -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!-- 格式化输出：
                 %d{...}       → 日期时间
                 %thread       → 线程名
                 %-5level      → 日志级别，左对齐显示5个字符宽度
                 %logger{50}   → 日志记录器名称，最长50个字符（超出则截断）
                 %msg          → 日志消息
                 %n            → 换行
            -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
    <!-- 日志文件输出的文件名，%d 表示日期，%i 表示序号 -->
    <FileNamePattern>${user.home}/Desktop/java/tlias/tlias-%d{yyyy-MM-dd}-%i.log</FileNamePattern>

    <!-- 最多保留的历史日志文件数量 -->
    <MaxHistory>30</MaxHistory>

    <!-- 最大文件大小，超过这个大小会触发滚动到新文件，默认为 10MB -->
    <maxFileSize>10MB</maxFileSize>
</rollingPolicy>

    <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!-- 格式化输出：
                 %d{...}       → 日期时间
                 %thread       → 线程名
                 %-5level      → 日志级别，左对齐显示5个字符宽度
                 %logger{50}   → 日志记录器名称，最长50个字符（超出则截断）
                 %msg          → 日志消息
                 %n            → 换行
            -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
        </encoder>
    </appender>
    <!-- 日志输出级别 常用的是info、warn、error,开发过程中使用debug-->
    <root level="debug">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="FILE" />
    </root>

</configuration>